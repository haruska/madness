<header>
  <div class="title-wrapper">
    <div class="title">
      <% if content_for?(:title) %>
        <%= yield :title %>
      <% else %>
        Pool Madness
      <% end %>
    </div>
  </div>
  <div class="navigation">
    <%= link_to("Users", users_path) if policy(User).index? %>
    <%= link_to("Sign Out", sign_out_path) if current_user.present? %>
  </div>
</header>